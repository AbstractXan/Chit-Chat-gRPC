syntax = "proto3";

package proto;


// Types
message Message{
    string sender = 1;
    string text = 2;
    bool register = 3;
    int32 group = 4;
}

// Services
service Chat{
    rpc TransferMessage(stream Message) returns (stream Message) {}
}
